import styled from "styled-components"
import { BLACK } from "../../constants"

const CreateSystemPageStyled = styled.div`
    width: 100%;
    height: 100vh;
    background-color: ${BLACK};
    display: flex;
    flex-direction: row;
`

export {CreateSystemPageStyled}